# Project-specific R8 rules.
#
# Library rules for Compose, CameraX, ML Kit, OkHttp and kotlinx.serialization are
# provided by their own consumer ProGuard files. Keep this file for app-level
# exceptions when a future feature uses reflection or dynamically loaded classes.
