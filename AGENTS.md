# 开发约定

1. 使用中文回答。
2. 仅清理本次新增的临时测试和调试文件，保留项目已有测试及必要的回归测试。
3. 提交前查看当前项目大部分提交使用的语言，并沿用该语言。
4. 最小范围修改，不增加要求以外的功能、重构或抽象，不为不可能发生的场景编写防御代码。
5. 不将开发细节展示在用户界面；必要说明使用普通用户能理解的语言。
6. 如果修改涉及界面文案，同步修改该客户端已有的国际化资源。
7. 异步任务避免频繁轮询和重复报告无变化，遵循工具提供的等待机制。
8. 发布说明只写修复了什么问题或新增了什么功能；不加入开发过程、技术实现、测试记录、安装指引、签名说明或其他无关说明。
9. 新增或修改页面、组件、按钮及其他界面模块时，必须沿用相应客户端现有的主题、配色、字体、间距、圆角和交互风格，优先复用已有样式与组件，并保持浅色、深色及现有皮肤下的一致性；不得擅自使用引入的组件库、框架或模板的默认样式，接入后须适配现有 App 风格。

## 仓库边界

- 四端源码位于 `apps/web`、`apps/macos`、`apps/android`、`apps/ios`，保持各自独立的构建配置。
- 在相应客户端目录执行原有脚本；也可使用根目录 Makefile 的入口。
- 涉及数据字段、加密、同步协议的变更需检查四端兼容性，不顺带抽取共享实现。
- 保留应用标识和本地数据位置；签名轮换必须获得明确授权，不能通过关闭验证来兼容已泄漏密钥。
- 本仓库保持公开。禁止提交密钥、签名文件、明文凭据或机器专用配置；发布密钥只经 GitHub Secrets / 仓库外的私有环境注入。不得复用历史中已泄漏的 Android 签名。
- 安全整改参见 `docs/signing-security.md`；普通删除提交不能替代凭据轮换或历史清理。提交前运行 `python3 scripts/check-signing-secrets.py`（检查 Git 索引，不输出凭据）。

## macOS 本地安全存储（维护者已确认，2026-09-19）

- macOS 默认并持续使用现有本地加密文件存储卡片、同步账本和本机凭证；不使用系统钥匙串（Keychain）作为正常存储后端，不新增、恢复或迁移为钥匙串存储，也不以整改为由强制替换成密码派生保险库。
- 本地密钥与密文按现有目录和权限保存，是维护者接受的设计边界，不是待修复事项。后续审查、修复计划和自动化任务不得仅因此再次安排 Keychain/安全存储迁移；只有维护者另行明确改变此决定时才能重新设计。
- 保留现有加密、随机密钥、文件权限、原子写入和失败保护；不得改成明文、删除旧密钥或清空用户数据。真实的泄漏、损坏、错误覆盖及实现缺陷仍需正常处理，不因本约定被忽略。
- `KeychainManager` 的历史名称及已有的旧版本单向迁入兼容，不代表当前存储后端是 Keychain；不要因名称恢复钥匙串读写，也不要未经授权删除旧凭证。锁定时释放内存和后台读盘优化属于独立问题，可在不改变磁盘存储模式的前提下修复。
- 本约定仅限定 macOS 应用本地存储，不要求替换 iOS 的既有存储或 Android Keystore；发布签名继续使用现有 GitHub Secrets，与应用本地数据密钥分开。详情见 [macOS 说明](apps/macos/README.md)；剩余任务见 [四端复审与修复计划](docs/code-review-20260919.md)。
