# 四阶段执行记录（2026-09-19）

依据 `docs/code-review-20260919.md` 的四阶段计划执行。macOS 始终保留现有本地加密文件、密钥和路径；不新增钥匙串存储、不改变 SyncV4、应用标识或发布签名。

## 阶段一：iOS 锁定会话与恢复

状态：代码与隔离回归完成。提交前运行 `35446542356` 的 iOS 原生构建及 27 项测试全部通过（含新增 9 项会话测试），导出的源文件对象与审阅版本逐一核对。一阶段提交移除临时验证 job，仍只保留四个长期工作流。

实现独立的 Locked/Loading/Ready/Error 加载状态。冷启动锁屏不读卡，卡片与必要账本恢复成功后才显示可编辑钱包；历史失败不隐藏健康的卡片。锁定立即撤销会话、取消在途任务/请求和计时器，释放 cards/ledger/history；空的锁定状态不落盘。旧会话的同步结果、错误处理与进度回调不能在下一次解锁后修改内存或文件。通知在系统授权等待前投影为日期、ID 和数量，不跨等待持有完整卡片；已排程通用通知保留。图片导入/识别任务在离开页面时取消并验证原会话，卡号计时器离开时释放。

新增隔离存储测试覆盖冷锁无读写、锁定释放且不写空库、离线恢复墓碑和待上传标记、旧会话拒绝、读卡/账本错误、历史失败、真实空钱包及重复恢复。保留既有存储格式和 iOS 存储后端。只承诺释放应用可控的引用；系统/图像/密码库中已运行的短时操作需要返回后释放临时副本，不宣称逐字节擦除。

阶段二：待执行（Apple 本地读写性能）。阶段三：待执行（Android PIN）。阶段四：待执行（综合回归与交付）。

真实设备的 Touch ID/Face ID、系统授权以及大图片性能对比仍需设备验收；自动化测试不能代替这些结果。
