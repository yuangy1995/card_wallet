# macOS 原生信用卡管理客户端 ── 细致开发与交互设计计划表

本项目采用 **Swift 5.10 + SwiftUI** 构建符合 macOS 操作系统设计哲学（HIG 规范）的原生桌面应用。
我们将保持**极简、简单明了、高对比度科技暗黑风**的 UI 质感，同时确保界面符合人类的第一直觉与日常操作逻辑。

---

## 🎨 人机交互（HIG）与科技审美准则

1. **科技感不等于杂乱**: 科技感是精致的描边、细腻的渐变和流畅的过渡动画，而不是堆砌复杂的按钮。主界面必须极度干净，将次要操作收拢于右键快捷菜单（ContextMenu）和原生滑动手势中。
2. **黄金卡片比例**: 所有信用卡磁贴统一采用实体卡片的 **1:1.586** 黄金宽高比例，并提供圆角与精致的磨砂卡面纹理，提供最真实而又充满未来感的心智映射。
3. **物理动画隐喻**: 鼠标 Hover 卡片时，卡片轻微浮起（Offset 与 Scale 缩放），并伴有与光标同步的霓虹光晕位移，提供极佳的手感反馈。

---

## 📋 每一个 Web 端功能在 macOS 端的极细致实现方案

### 1. 核心卡包主界面与交互 (The Card Wallet Grid)
*   **Web 实现**: 用 Element Plus 的表格展示卡片信息，通过列显示隐藏进行响应式适配，展示额度、下次提额时间等。
*   **macOS 原生人机实现**:
    *   **布局逻辑**: 抛弃冰冷的复杂大表格，默认采用符合人类审美的 **“精美卡包网格视图 (Grid Card View)”**。通过 SwiftUI `ScrollView` 与 `LazyVGrid` 渲染高对比度金属卡片。卡片左侧为银行与卡类别，中间为卡号，底部为额度与免息剩余天数。
    *   **悬浮 3D 动效**: SwiftUI 中利用 `.rotation3DEffect` 与 `.onHover`，当鼠标移入某张卡片时，卡片随光标微幅倾斜，边缘展现霓虹流光，直观告知用户“此项目可点击操作”。
    *   **快速切换视图**: 依然在顶部导航栏保留一个切换按钮，可一键切换为 macOS 原生的 `Table` 列表视图（支持多列自定义排序、列宽度拖拽），满足习惯看紧凑表格的高级用户。

### 2. 敏感信息（卡号、CVV）的显示与时限遮罩
*   **Web 实现**: 敏感字段（卡号/CVV）支持小眼睛切换，依靠 JavaScript 定时器在显示 5 秒或 10 秒后自动重新遮罩。
*   **macOS 原生人机实现**:
    *   **防窥机制**: 卡号默认遮罩显示为 `•••• •••• •••• 1234`，CVV 显示为 `•••`。
    *   **人机反馈**: 点击卡片上的“小眼睛”或右键菜单选择明文显示。卡号在平滑淡入显示的瞬间，旁边会出现一个非常精致的 **“环形倒计时进度条 (Circle Countdown)”**，随着 5 秒时间流逝慢慢缩减，并伴随柔和的淡出遮罩。
    *   **多卡防窥**: 系统同一时间只允许明文显示一张卡的敏感数据。当显示卡 B 时，正在倒计时的卡 A 自动瞬间回弹为遮罩状态，完美符合人类的安全防窥逻辑。

### 3. 新建与编辑信用卡表单 (Add/Edit Form)
*   **Web 实现**: 使用巨大的 Element Plus Descriptions 对话框，填报字段非常繁杂，选择额度共享与年费达标时，排版较乱。
*   **macOS 原生人机实现**:
    *   **表单布局**: 采用 macOS 标准的 **Sheet 弹窗**，将表单分类规整为 4 个符合人类信息整理逻辑的 `Section`：
        1. **🌏 基础信息**: 国家（带智能首字母拼音过滤选择器）、银行（自动识别中文/英文）、卡片别名、等级。
        2. **💳 卡片安全**: 卡号（支持 `format` 实时自动按 `4 4 4 4` 空格排版，并在右上角实时渲染对应品牌的矢量卡标）、CVV（密码格式输入）、有效期（标准的月/年滑动选择轮）。
        3. **💰 额度与年费**: 币种（带图标的 Picker）、账单日与还款日（提供 1-31 的步进器）、共享额度单选、年费额度。
        4. **🎁 权益与备注**: 宽大的文本框，支持自动高度。
    *   **快捷焦点流**: 完美支持 `Tab` 键顺畅向下切换焦点，输入框支持按回车键直接跳转到下一个输入框，表单底部支持 `Escape` 取消，`⌘Enter` 确定保存。

### 4. 共享额度 (Limit Sharing) 的智能匹配与警告
*   **Web 实现**: 输入国家、银行时，如果选择共享额度，自动在已有卡片中查找额度并覆盖，提示修改额度会影响其他卡。
*   **macOS 原生人机实现**:
    *   **智能锁定**: 当用户在表单里开启“是，共享额度”时，输入框下方立刻弹出一行优雅的青色提示语：*“已自动应用同银行共享额度组：$50,000 (CNY)”*，此时额度输入框会自动锁定，不允许用户在单一卡片上单独修改，规避人类的误操作。
    *   **全局修改警告**: 如果用户执意要修改共享额度，需要点击旁边的“修改共享总额”按钮，此时表单会呈现强烈的黄色微光提示，并在保存时拉起符合 HIG 的 `NSAlert` 警告弹窗：*“修改此额度将一并更新该行旗下另外 3 张共享卡的额度。您确定要全局同步吗？”*。

### 5. 年费达标管理与右键快捷标记 (Annual Fee & ContextMenu)
*   **Web 实现**: 列表中点击修改，或者行右键菜单修改年费达标状态。自动检测临近年费扣除日卡片并高亮。
*   **macOS 原生人机实现**:
    *   **视觉归纳**: 距离下次收年费 **不足 60 天** 的卡片，在卡片右上角闪烁一个柔和的橙色指示灯。
    *   **一键标记 (ContextMenu)**: 极佳的人类操作逻辑！用户无需点击“编辑表单”再去修改选项，在主卡包视图下，**直接右键**点击卡片，弹出高对比度的 macOS 原生 `contextMenu`：
        *   `✅ 标记年费为已达标`
        *   `⚠️ 标记年费为未达标`
        *   `♾️ 标记为终身免年费`
        *   `------------------` (分割线)
        *   `✏️ 编辑卡片信息 (⌘E)`
        *   `🗑️ 删除此卡片 (⌘Delete)`
    *   **急需处理归类**: 左侧 Sidebar 设立“即将收取年费”智能过滤器，并配有红色的数字角标（如 `③`），提醒用户有 3 张卡年费急需跟进，一目了然。

### 6. 数据本地多版本自动备份与“差异差异比对 (Diff)”预览
*   **Web 实现**: 每次数据修改后 1 分钟自动备份（上限 50 条），并在本地备份对话框中列出备份、查看备份数据和进行差异对比。
*   **macOS 原生人机实现**:
    *   **自动静默备份**: 在后台开启线程，监测到 `cards.json` 数据变动 1 分钟后自动保存副本至 `~/Library/Application Support/CreditCardMac/Backups/`。
    *   **符合人类直觉的 Diff 面板**: 当用户打开“数据恢复”界面并选中某个备份点时，客户端不会只生硬地展示 JSON，而是弹出一个双栏 **“内容比对 (Diff) 面板”**：
        *   🟩 **绿色背景高光**: 备份中存在，而当前没有的卡片（恢复后将新增）。
        *   🟥 **红色背景高光**: 当前存在，而备份中没有的卡片（恢复后将丢失，高亮提醒）。
        *   🟨 **黄色背景高光**: 卡片中的具体改动字段（例如：*广发卡别名: "网购卡" ➔ "常用卡"*）。
    *   这个界面极大地缓解了用户恢复旧备份时的“数据丢失焦虑”，极其贴心。

### 7. 数据安全导入与导出 (带有密码保护的 NSSavePanel)
*   **Web 实现**: 导入导出加密的 JSON 文件，支持自定义输入密码，通过 CryptoJS 处理。
*   **macOS 原生人机实现**:
    *   **原生导出文件**: 点击“导出”按钮，调用系统级 `NSSavePanel`。在弹窗中询问用户：“是否使用密码加密此导出文件？”
        *   若不加密：直接写盘普通 JSON。
        *   若加密：输入密码并伴有圆滑的密码强度彩色进度条，写盘为受 CryptoJS 兼容算法强加密的 `.json` 数据。
    *   **原生导入文件**: 点击“导入”，调用系统级 `NSOpenPanel` 过滤 `.json` 文件。选择文件后，应用后台会自动读取前缀。若发现 `encrypted:` 前缀，会自动弹出精美的输入密码面板。输入正确密码后自动解析并提供“导入卡片预览列表”，允许用户勾选要导入的卡片，防止垃圾数据污染本地卡包。

### 8. WebDAV 云端备份双向管理
*   **Web 实现**: 输入 WebDAV 配置，保存配置（本地加密），支持测试连接、创建备份、展示备份列表、删除和重命名备份。
*   **macOS 原生人机实现**:
    *   **无缝 Keychain 集成**: 彻底取消本地 LocalStorage 明文或简单加密保存配置的漏洞。配置 WebDAV 账户密码时，用户名和应用独立密码将直接交由系统内置的 **钥匙串 (Keychain)** 接管，享受 macOS 硬件芯片的安全保障。
    *   **云备份文件管理器**: 专设一个简洁高雅的云盘控制台，卡片式展示备份列表：
        *   每一行左侧显示云端备份的文件名、卡片数量和备份日期；
        *   右侧提供鼠标 Hover 时才显式的 `📥 恢复数据` 与 `🗑️ 删除备份` 极简科技感按钮。
        *   支持双击备份文件名，直接在行内变成输入框，回车即可调用 HTTP `MOVE` 报文对云端备份进行重命名，逻辑非常原生。

### 9. 智能数据统计与原生可视化 (Statistics Engine)
*   **Web 实现**: 引入 ECharts 库，展示银行分布、币种分布、年费达标占比等，可自定义配置列。
*   **macOS 原生人机实现**:
    *   **原生 Charts 替代**: 绝不生硬套用网页图表。我们使用 macOS 原生的 **SwiftUI Charts** 框架进行手绘：
        *   **额度结构环形图**: 完美地区分出 **独立额度** 与 **共享额度** 进行加权占比计算，用精细渐变色的圆环（Donut Chart）高雅呈现。
        *   **银行占比水平堆叠图**: 极具现代科技感的横向条形占比图，配有微缩银行卡标。
        *   **年费情况饼图**: 呈现已达标、未达标、终免年费的占比，完美贴合系统深色模式。
    *   **数据导出**: 支持一键将统计面板的所有数据表格直接导出为 `.csv` 报文（调用 NSSavePanel）。

### 10. 闲置自动锁定与防窥护盾 (Touch ID Ready)
*   **Web 实现**: 5 分钟无操作自动锁定，手动锁定/解锁，忘记密码找回等。
*   **macOS 原生人机实现**:
    *   **磨砂防窥罩**: 当应用闲置 5 分钟无操作，或应用从后台再次唤醒时，主窗口上方会以平滑的动画拉下一层 **“.ultraThinMaterial” 强磨砂防窥罩**。界面瞬间模糊，防止屏幕被旁人窥视。
    *   **Touch ID 原生秒解锁**: 模糊罩中央浮现一枚散发着紫色霓虹光圈的指纹图标。用户只需手指轻轻按压 Mac 键盘上的 **Touch ID**，即可在 0.1 秒内平滑淡出磨砂罩，恢复操作。若设备不支持 Touch ID，则可点击下方“输入安全密码”完成秒级解锁。

---

## 🚀 细致的开发实施任务步骤表 (Task Timeline)

### 第一阶段：项目初始化与安全加解密网关 (Phase 1)
- [x] **根目录开发计划定稿**: 在工作区根目录成功创建 `DEVELOPMENT_PLAN.md` 详细对照计划表。
- [ ] **项目骨架搭建**:
  - [ ] 编写 `project.yml` 配置，加入 `CryptoSwift` 原生 Swift Package SPM 依赖。
  - [ ] 执行 `xcodegen generate`，一键拉起 Xcode 编译工程。
- [ ] **数据模型与迁移器设计**:
  - [ ] 建立 `CardModels.swift`（符合 `SharedCard` 基线，定义 7 大卡组织品牌枚举）。
  - [ ] 编写 `DataMigrationManager.swift`，百分百还原 Web 端 8 大迁移规则。
- [ ] **AES CryptoJS Bridge 加解密**:
  - [ ] 编写 `CryptoManager.swift`，手动利用 `CryptoSwift` 轮转 MD5 衍生 Key & IV，兼容解密 `default:` 和 `encrypted:` 前缀密文。
  - [ ] 编写全面的加解密单元测试用例，完美通过对 Web 端所产生密文的测试。

### 第二阶段：安全钥匙串与 WebDAV 同步引擎 (Phase 2)
- [ ] **系统钥匙串托管**: 编写 `KeychainManager.swift`，用于将 WebDAV 账号密码安全存入系统 Keyring。
- [ ] **原生 WebDAV 同步**: 编写 `WebDAVClient.swift`，使用 Swift 原生 `URLSession` 与 `Basic Auth` 头部传输数据，支持 PROPFIND/PUT/GET/DELETE/MOVE。
- [ ] **本地自动备份机制**: 实现 `LocalStorageManager.swift` 读写卡片 JSON，并添加修改 1 分钟后自动后台多版本静默备份（上限 50 条）。
- [ ] **前后台自动锁屏**: 引入 `AutoLockManager.swift`，接入 `LocalAuthentication` 原生 Touch ID。

### 第三阶段：高对比度科技卡片与人类直觉交互 (Phase 3)
- [ ] **主框架 UI (Sidebar & Navigation)**: 构建带有左侧三栏式磨砂玻璃归类 Sidebar，支持国家、银行、年费临近角标。
- [ ] **手绘 7 大品牌矢量卡标**: 实现 `CardBrandIcon.swift` 纯代码完美渲染 Visa, MC, Amex, Diners, Discover, JCB, 银联卡标。
- [ ] **卡片网格界面 (Grid Wallet)**: 渲染 1:1.586 黄金卡包，支持 Hover 浮起与流光溢彩，并实现卡片上 CVV/卡号 5 秒智能自动环形倒计时进度条隐藏功能。
- [ ] **智能表单 Sheets**: 实现流畅的卡片编辑表单，集成额度共享联动警告与卡号输入卡组织高精度前缀侦测。
- [ ] **快速右键菜单 (ContextMenu)**: 行右键一键快速更改年费达标状态。

### 第四阶段：数据分析、多版本 Diff 比对与交付 (Phase 4)
- [ ] **原生数据可视化**: 编写 `StatisticsView.swift`，采用 SwiftUI `Charts` 表现精美额度分类环形图与银行占比。
- [ ] **数据多版本 Diff 预览比对**: 编写备份对比界面，用红黄绿高光直观呈现备份差异。
- [ ] **全流程联调测试与 Walkthrough**:
  - [ ] WebDAV 跨端无损数据合流同步测试。
  - [ ] 生成 `walkthrough.md` 项目交付总结报告与截图展示。
